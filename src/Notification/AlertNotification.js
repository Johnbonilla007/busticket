import React from 'react';
import AlertNotification from "../controls/Alert";
import { render } from 'react-dom';

export class AlertNotificationTest {
    static success = (content) => {
        const alert = document.getElementById('alert-notification');
        debugger

        // const test = React.createElement(alert, {content, open: true});
        
        // alert.innerHTML = <AlertNotification content={content} />

        // render(test);
    }
}